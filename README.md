example kernel module, compile with `make`, load with `insmod hello.ko` and remove it with `rmmod hello`.
Insertion & removal will print log messages (`Hello, world` and `Goodbye, cruel world` to kernel log).
